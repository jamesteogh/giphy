import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App/>, document.getElementById('root'));

// use createRoot instead of ReactDom - import ReactDOM from 'react-dom';